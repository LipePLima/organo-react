const Form = () => {
    return (
        <section>
            <form>
                <h2>Preencha os dados para criar o card do colaborador.</h2>
                <fieldset>
                    <legend>Nome</legend>
                    <input type="text" id="nome"/>
                </fieldset>
            </form>
        </section>
    )
}

export default Form;